interface:
  display_name: "LEGO Model Builder"
  short_description: "Build and verify LEGO models as LDraw files"
  default_prompt: "Use $lego-model-builder to turn my description into a reusable pyldraw3 program and verified LDraw model."
